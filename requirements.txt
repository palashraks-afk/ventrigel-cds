# Pinned to the versions the reported results were produced with.
# Loosen at your own risk: scipy's noncentral t is load-bearing here.
numpy==2.3.5
pandas==2.3.3
scipy==1.17.1
matplotlib==3.10.8
plotly==6.5.0
streamlit==1.59.2
